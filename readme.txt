两端的程序都已被打包为可执行jar包。
环境：openjdk11

1.client运行方法：
	clone下来后在命令行运行client.jar:(端口固定为10087)
		java -jar client.jar <serverIP> 10087

2.server运行方法：
	clone下来后在命令行运行server.jar:(端口固定为10087)
		java -jar server.jar

关闭：通过ctrl+c关闭。